# CRO Talk — Palestra 02 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce todos os artefatos necessários para a palestra 02 (CRO): demo skills, hook de validação, pipeline de dados com DuckDB, e script narrativo completo.

**Architecture:** Três subsistemas independentes — (A) demo skills em `02-cro/demos/skills/` são arquivos markdown seguindo o mesmo formato do `ecommerce-skills`; (B) dados de demo usam DuckDB como cache local alimentado por `gmp-cli`; (C) conteúdo narrativo (`script.md`, `bifurcations.md`) segue o padrão da palestra 01. Podem ser executados em qualquer ordem.

**Tech Stack:** Claude Code skills (markdown com frontmatter), DuckDB (SQL), JSON config para hooks `.claude/settings.json`, bash scripts para demo runbook.

**Spec:** `02-cro/script-outline.md`

---

## File Map

```
02-cro/
├── script-outline.md          ✅ EXISTS
├── script.md                  Task 9 — narrativa completa (5 atos)
├── bifurcations.md            Task 8 — 3 pontos de bifurcação
├── demos/
│   ├── skills/
│   │   ├── srm-check.md               Task 3
│   │   ├── post-test-segments.md      Task 4
│   │   ├── cro-weekly-pipeline.md     Task 5
│   │   └── heuristic-scan.md          Task 6
│   ├── hooks/
│   │   └── pre-tool-use-validation.json  Task 7
│   ├── data/
│   │   ├── setup-duckdb.sql           Task 2
│   │   └── sample-ga4-events.csv      Task 2
│   └── commands.md                    Task 8 (runbook ao vivo)
└── notes/
    └── rehearsal-log.md               Task 10 (template)
```

---

## Task 1: Scaffold da estrutura de pastas

**Files:**
- Create: `02-cro/demos/skills/.gitkeep`
- Create: `02-cro/demos/hooks/.gitkeep`
- Create: `02-cro/demos/data/.gitkeep`
- Create: `02-cro/notes/.gitkeep`

- [ ] **Step 1: Criar estrutura de diretórios**

```bash
mkdir -p 02-cro/demos/skills
mkdir -p 02-cro/demos/hooks
mkdir -p 02-cro/demos/data
mkdir -p 02-cro/notes
touch 02-cro/demos/skills/.gitkeep
touch 02-cro/demos/hooks/.gitkeep
touch 02-cro/demos/data/.gitkeep
touch 02-cro/notes/.gitkeep
```

Expected: sem erros, diretórios criados.

- [ ] **Step 2: Verificar estrutura**

```bash
find 02-cro -type d | sort
```

Expected output:
```
02-cro
02-cro/demos
02-cro/demos/data
02-cro/demos/hooks
02-cro/demos/skills
02-cro/notes
```

- [ ] **Step 3: Commit**

```bash
git add 02-cro/demos 02-cro/notes
git commit -m "chore: scaffold 02-cro directory structure"
```

---

## Task 2: Dataset mockado de GA4 + setup DuckDB

O dataset precisa ser realista o suficiente para as demos (bot filtering, SRM check, segmentação pós-teste) mas não pode conter dados reais de clientes. ~10k linhas é suficiente para a demo.

**Files:**
- Create: `02-cro/demos/data/sample-ga4-events.csv`
- Create: `02-cro/demos/data/setup-duckdb.sql`
- Create: `02-cro/demos/data/generate-sample-data.py`

- [ ] **Step 1: Criar script gerador de dados mockados**

Create `02-cro/demos/data/generate-sample-data.py`:

```python
import csv
import random
import datetime

random.seed(42)

VARIANTS = ['control', 'variant_b']
DEVICES = ['mobile', 'desktop', 'tablet']
CHANNELS = ['organic', 'cpc', 'social', 'email', 'direct']
BOT_IDS = [f'bot_{i:04d}' for i in range(200)]
REAL_USERS = [f'user_{i:06d}' for i in range(8000)]

rows = []
start = datetime.datetime(2026, 3, 1)

for i, user_id in enumerate(REAL_USERS):
    device = random.choice(DEVICES)
    channel = random.choice(CHANNELS)
    variant = VARIANTS[i % 2]
    session_duration = random.randint(30, 600)
    num_events = random.randint(2, 15)
    converted = (
        random.random() < (0.045 if variant == 'control' else
                           0.054 if device == 'mobile' else 0.046)
    )
    ts = start + datetime.timedelta(
        days=random.randint(0, 29),
        seconds=random.randint(0, 86400)
    )
    rows.append({
        'user_pseudo_id': user_id,
        'event_name': 'session_start',
        'event_timestamp': ts.isoformat(),
        'device_category': device,
        'traffic_source': channel,
        'experiment_variant': variant,
        'session_duration': session_duration,
        'converted': int(converted),
        'is_new_user': int(random.random() < 0.4),
    })
    for _ in range(num_events - 1):
        rows.append({
            'user_pseudo_id': user_id,
            'event_name': random.choice(['page_view', 'scroll', 'click', 'add_to_cart']),
            'event_timestamp': (ts + datetime.timedelta(seconds=random.randint(1, session_duration))).isoformat(),
            'device_category': device,
            'traffic_source': channel,
            'experiment_variant': variant,
            'session_duration': session_duration,
            'converted': int(converted),
            'is_new_user': int(random.random() < 0.4),
        })

# injetar bots (muitos hits, session_duration = 0)
for bot_id in BOT_IDS:
    for _ in range(random.randint(60, 120)):
        rows.append({
            'user_pseudo_id': bot_id,
            'event_name': 'session_start',
            'event_timestamp': (start + datetime.timedelta(days=random.randint(0, 29))).isoformat(),
            'device_category': 'desktop',
            'traffic_source': 'direct',
            'experiment_variant': random.choice(VARIANTS),
            'session_duration': 0,
            'converted': 0,
            'is_new_user': 0,
        })

# injetar SRM: variant_b recebe 23% mais tráfego que control nos primeiros 5 dias
extra_variant_b = [f'srm_user_{i:04d}' for i in range(400)]
for uid in extra_variant_b:
    ts = start + datetime.timedelta(days=random.randint(0, 4), seconds=random.randint(0, 86400))
    rows.append({
        'user_pseudo_id': uid,
        'event_name': 'session_start',
        'event_timestamp': ts.isoformat(),
        'device_category': random.choice(DEVICES),
        'traffic_source': random.choice(CHANNELS),
        'experiment_variant': 'variant_b',
        'session_duration': random.randint(10, 300),
        'converted': 0,
        'is_new_user': 1,
    })

random.shuffle(rows)

with open('02-cro/demos/data/sample-ga4-events.csv', 'w', newline='') as f:
    writer = csv.DictWriter(f, fieldnames=rows[0].keys())
    writer.writeheader()
    writer.writerows(rows)

print(f"Generated {len(rows)} rows")
print(f"  Real users: {len(REAL_USERS)}")
print(f"  Bot IDs injected: {len(BOT_IDS)}")
print(f"  SRM users injected: {len(extra_variant_b)}")
```

- [ ] **Step 2: Gerar o CSV**

```bash
cd /Users/lucianfialho/Code/talks-2026
python3 02-cro/demos/data/generate-sample-data.py
```

Expected output:
```
Generated ~110000 rows
  Real users: 8000
  Bot IDs injected: 200
  SRM users injected: 400
```

- [ ] **Step 3: Criar setup-duckdb.sql**

Create `02-cro/demos/data/setup-duckdb.sql`:

```sql
-- Bootstrap do ambiente de demo CRO
-- Roda uma vez antes das demos: duckdb cro.db < setup-duckdb.sql

CREATE TABLE IF NOT EXISTS events AS
SELECT * FROM read_csv_auto('02-cro/demos/data/sample-ga4-events.csv');

-- view de sessões únicas (uma linha por usuário)
CREATE OR REPLACE VIEW sessions AS
SELECT
    user_pseudo_id,
    COUNT(*) AS hit_count,
    MAX(CASE WHEN event_name = 'session_start' THEN session_duration END) AS session_duration,
    MAX(device_category) AS device_category,
    MAX(traffic_source) AS traffic_source,
    MAX(experiment_variant) AS experiment_variant,
    MAX(converted) AS converted,
    MAX(is_new_user) AS is_new_user,
    MIN(event_timestamp) AS first_seen
FROM events
GROUP BY user_pseudo_id;

-- view sem bots (usada após Task 3 rodar)
CREATE OR REPLACE VIEW events_clean AS
SELECT e.*
FROM events e
WHERE e.user_pseudo_id NOT IN (
    SELECT user_pseudo_id FROM sessions
    WHERE hit_count > 50 OR session_duration = 0
);

SELECT 'Setup completo.' AS status,
       COUNT(DISTINCT user_pseudo_id) AS total_users,
       COUNT(*) AS total_events
FROM events;
```

- [ ] **Step 4: Testar setup**

```bash
cd /Users/lucianfialho/Code/talks-2026
duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql
```

Expected output:
```
┌────────────────┬─────────────┬──────────────┐
│     status     │ total_users │ total_events │
│    varchar     │    int64    │    int64     │
├────────────────┼─────────────┼──────────────┤
│ Setup completo.│    8600+    │   110000+    │
└────────────────┴─────────────┴──────────────┘
```

- [ ] **Step 5: Commit**

```bash
git add 02-cro/demos/data/
git commit -m "feat: add mock GA4 dataset and DuckDB setup for CRO demos"
```

---

## Task 3: Skill `/srm-check`

SRM = Sample Ratio Mismatch. Skill que valida integridade de distribuição de tráfego antes de qualquer análise de teste A/B.

**Files:**
- Create: `02-cro/demos/skills/srm-check.md`

- [ ] **Step 1: Criar a skill**

Create `02-cro/demos/skills/srm-check.md`:

```markdown
---
name: srm-check
description: Valida Sample Ratio Mismatch em testes A/B antes de declarar resultado. Usa chi-quadrado pra detectar distribuição desproporcional de tráfego entre variantes.
---

## Tarefa

Verificar se o teste A/B tem SRM (Sample Ratio Mismatch) antes de qualquer análise de resultado.

## Passos

1. Buscar no DuckDB (arquivo `cro.db` na pasta do projeto) a distribuição de usuários únicos por variante usando `events_clean`:

```sql
SELECT
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    ROUND(COUNT(DISTINCT user_pseudo_id) * 100.0 /
          SUM(COUNT(DISTINCT user_pseudo_id)) OVER (), 2) AS pct
FROM events_clean
WHERE experiment_variant IS NOT NULL
GROUP BY experiment_variant
ORDER BY experiment_variant;
```

2. Calcular chi-quadrado:
   - Esperado: distribuição igual entre variantes (50/50 para teste simples A/B)
   - Fórmula: χ² = Σ (observado - esperado)² / esperado
   - p-value crítico: 0.05 (χ² > 3.84 para 1 grau de liberdade = SRM detectado)

3. Emitir veredicto claro:

**SRM DETECTADO:** Se χ² > 3.84
> "⚠️ SRM detectado. Variante B recebeu [X]% mais tráfego que control. Diferença: [N] usuários. χ² = [valor], p < 0.05. Este teste NÃO pode ser declarado vencedor — o dado de exposição está contaminado."

**SEM SRM:** Se χ² ≤ 3.84
> "✅ Sem SRM. Distribuição: control=[N] ([X]%), variant_b=[N] ([X]%). χ² = [valor], p > 0.05. Pode prosseguir com a análise."

4. Se SRM detectado, sugerir causas prováveis:
   - Implementação da variante com delay (JS que carrega tarde)
   - Segmento de exclusão não aplicado em uma das variantes
   - Cache de CDN servindo versão errada
   - Bot traffic concentrado em uma variante

## Contexto esperado

- DuckDB em `cro.db` na pasta do projeto com a view `events_clean` criada por `setup-duckdb.sql`
- Se o arquivo não existir, instruir o usuário a rodar: `duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql`
```

- [ ] **Step 2: Testar a skill invocando via Claude Code**

No terminal, dentro de `/Users/lucianfialho/Code/talks-2026`:

```bash
# garantir que cro.db existe com os dados
duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql

# invocar a skill (simular como Claude Code faria)
# verificar manualmente que a query produz output com SRM detectado
duckdb cro.db "
SELECT
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    ROUND(COUNT(DISTINCT user_pseudo_id) * 100.0 /
          SUM(COUNT(DISTINCT user_pseudo_id)) OVER (), 2) AS pct
FROM events_clean
WHERE experiment_variant IS NOT NULL
GROUP BY experiment_variant;
"
```

Expected: variant_b deve ter ~23% mais usuários que control nos primeiros dias (SRM injetado no gerador). O output deve mostrar distribuição desigual.

- [ ] **Step 3: Commit**

```bash
git add 02-cro/demos/skills/srm-check.md
git commit -m "feat: add /srm-check skill for CRO demo"
```

---

## Task 4: Skill `/post-test-segments`

Análise pós-teste com segmentação automática por device, novo/recorrente e canal de aquisição.

**Files:**
- Create: `02-cro/demos/skills/post-test-segments.md`

- [ ] **Step 1: Criar a skill**

Create `02-cro/demos/skills/post-test-segments.md`:

```markdown
---
name: post-test-segments
description: Análise pós-teste A/B com segmentação automática. Quebra o resultado agregado por device, novo vs recorrente e canal de aquisição. Revela onde o lift realmente aconteceu.
---

## Tarefa

Depois que um teste foi validado (sem SRM), fazer a análise segmentada completa para entender ONDE o lift aconteceu.

## Passos

1. Primeiro verificar se SRM check foi feito. Se não: invocar `/srm-check` antes de continuar.

2. Calcular conversion rate por variante (resultado agregado):

```sql
SELECT
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    SUM(converted) AS conversions,
    ROUND(SUM(converted) * 100.0 / COUNT(DISTINCT user_pseudo_id), 2) AS cr_pct
FROM events_clean
WHERE event_name = 'session_start'
GROUP BY experiment_variant;
```

3. Segmentação por device:

```sql
SELECT
    device_category,
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    SUM(converted) AS conversions,
    ROUND(SUM(converted) * 100.0 / COUNT(DISTINCT user_pseudo_id), 2) AS cr_pct
FROM events_clean
WHERE event_name = 'session_start'
GROUP BY device_category, experiment_variant
ORDER BY device_category, experiment_variant;
```

4. Segmentação por novo vs recorrente:

```sql
SELECT
    CASE WHEN is_new_user = 1 THEN 'new' ELSE 'returning' END AS user_type,
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    ROUND(SUM(converted) * 100.0 / COUNT(DISTINCT user_pseudo_id), 2) AS cr_pct
FROM events_clean
WHERE event_name = 'session_start'
GROUP BY user_type, experiment_variant
ORDER BY user_type, experiment_variant;
```

5. Segmentação por canal:

```sql
SELECT
    traffic_source,
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    ROUND(SUM(converted) * 100.0 / COUNT(DISTINCT user_pseudo_id), 2) AS cr_pct
FROM events_clean
WHERE event_name = 'session_start'
  AND COUNT(DISTINCT user_pseudo_id) OVER (PARTITION BY traffic_source) >= 100
GROUP BY traffic_source, experiment_variant
ORDER BY traffic_source, experiment_variant;
```

6. Emitir interpretação consolidada:

Formato esperado:
> **Resultado agregado:** control=[X]% vs variant_b=[Y]% → lift de [Z]%
>
> **Por device:**
> - Mobile: lift de [X]% (maior parte do tráfego)
> - Desktop: lift de [X]% (não significativo)
> - Tablet: amostra insuficiente
>
> **Insight:** O lift de [Z]% é puxado principalmente por [device/segmento]. Implementar para todos os devices entrega ~[real_lift]% de ganho real, não [Z]%.
>
> **Recomendação:** [implementar para mobile first / investigar desktop / aguardar mais dados em tablet]

## Contexto esperado

- DuckDB em `cro.db` com view `events_clean`
- `/srm-check` deve ter sido executado e aprovado antes desta skill
```

- [ ] **Step 2: Verificar queries contra o dataset**

```bash
duckdb cro.db "
SELECT
    device_category,
    experiment_variant,
    COUNT(DISTINCT user_pseudo_id) AS users,
    ROUND(SUM(converted) * 100.0 / COUNT(DISTINCT user_pseudo_id), 2) AS cr_pct
FROM events_clean
WHERE event_name = 'session_start'
GROUP BY device_category, experiment_variant
ORDER BY device_category, experiment_variant;
"
```

Expected: mobile/variant_b deve ter CR ~20% maior que mobile/control (conforme gerador). Desktop deve ter lift próximo de zero.

- [ ] **Step 3: Commit**

```bash
git add 02-cro/demos/skills/post-test-segments.md
git commit -m "feat: add /post-test-segments skill for CRO demo"
```

---

## Task 5: Skill `/cro-weekly-pipeline`

Pipeline semanal completo: puxa dados → limpa bots → SRM check → análise segmentada → relatório.

**Files:**
- Create: `02-cro/demos/skills/cro-weekly-pipeline.md`

- [ ] **Step 1: Criar a skill**

Create `02-cro/demos/skills/cro-weekly-pipeline.md`:

```markdown
---
name: cro-weekly-pipeline
description: Pipeline semanal completo de CRO. Puxa dados frescos via gmp-cli, limpa bots, valida SRM de testes ativos, roda análise segmentada e gera relatório em markdown pronto para compartilhar.
---

## Tarefa

Executar o pipeline completo de análise CRO para a semana atual.

## Passos

### 1. Puxar dados frescos

```bash
# puxar eventos GA4 dos últimos 7 dias via gmp-cli
gmp ga raw-events --days 7 --output 02-cro/demos/data/events-latest.csv

# se gmp-cli não estiver configurado, usar dataset de demo:
# cp 02-cro/demos/data/sample-ga4-events.csv 02-cro/demos/data/events-latest.csv
```

### 2. Atualizar DuckDB com dados frescos

```sql
-- rodar via: duckdb cro.db
DROP TABLE IF EXISTS events;

CREATE TABLE events AS
SELECT * FROM read_csv_auto('02-cro/demos/data/events-latest.csv');

-- recriar views
CREATE OR REPLACE VIEW sessions AS
SELECT
    user_pseudo_id,
    COUNT(*) AS hit_count,
    MAX(CASE WHEN event_name = 'session_start' THEN session_duration END) AS session_duration,
    MAX(device_category) AS device_category,
    MAX(traffic_source) AS traffic_source,
    MAX(experiment_variant) AS experiment_variant,
    MAX(converted) AS converted,
    MAX(is_new_user) AS is_new_user
FROM events GROUP BY user_pseudo_id;

CREATE OR REPLACE VIEW events_clean AS
SELECT e.* FROM events e
WHERE e.user_pseudo_id NOT IN (
    SELECT user_pseudo_id FROM sessions
    WHERE hit_count > 50 OR session_duration = 0
);

SELECT 'Dados atualizados.' AS status, COUNT(*) AS total_events FROM events;
```

### 3. Relatório de limpeza

```sql
SELECT
    'Total bruto' AS categoria,
    COUNT(DISTINCT user_pseudo_id) AS usuarios
FROM events
UNION ALL
SELECT
    'Bots removidos',
    COUNT(DISTINCT user_pseudo_id)
FROM sessions WHERE hit_count > 50 OR session_duration = 0
UNION ALL
SELECT
    'Dado limpo',
    COUNT(DISTINCT user_pseudo_id)
FROM events_clean WHERE event_name = 'session_start';
```

### 4. SRM check em testes ativos

Invocar `/srm-check` para cada teste ativo. Se SRM detectado em algum: marcar como ⚠️ no relatório e **não incluir resultado desse teste**.

### 5. Análise de testes sem SRM

Para cada teste aprovado, invocar `/post-test-segments`.

### 6. Gerar relatório markdown

Criar arquivo `02-cro/notes/weekly-report-[YYYY-MM-DD].md` com:

```markdown
# CRO Weekly Report — [data]

## Limpeza de dados
- Total bruto: [N] usuários
- Bots removidos: [N] ([X]%)
- Dado limpo: [N] usuários

## Testes ativos
### [Nome do teste]
- Status: ✅ Sem SRM / ⚠️ SRM detectado
- Resultado: [se sem SRM — lift + segmentação]
- Recomendação: [ação concreta]

## Próximos passos
- [lista]
```

## Contexto esperado

- `gmp-cli` instalado e configurado (ou dataset de demo disponível)
- DuckDB instalado: `brew install duckdb`
- Arquivo `cro.db` na pasta do projeto
```

- [ ] **Step 2: Verificar que o pipeline roda do início ao fim com dados mockados**

```bash
cd /Users/lucianfialho/Code/talks-2026
cp 02-cro/demos/data/sample-ga4-events.csv 02-cro/demos/data/events-latest.csv
duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql
```

Expected: sem erros, output com contagem de usuários.

- [ ] **Step 3: Commit**

```bash
git add 02-cro/demos/skills/cro-weekly-pipeline.md
git commit -m "feat: add /cro-weekly-pipeline skill for CRO demo"
```

---

## Task 6: Skill `/heuristic-scan`

Checklist de heurísticas visuais rodando via `agent-browser` numa URL de teste.

**Files:**
- Create: `02-cro/demos/skills/heuristic-scan.md`

- [ ] **Step 1: Criar a skill**

Create `02-cro/demos/skills/heuristic-scan.md`:

```markdown
---
name: heuristic-scan
description: Análise heurística visual de uma variante de teste A/B usando agent-browser. Verifica CTA acima da dobra, contraste, hierarquia visual, mobile UX e friction points. Complementa a análise quantitativa com hipóteses sobre o porquê do resultado.
---

## Tarefa

Dada uma URL de variante de teste, fazer varredura heurística automática e gerar hipóteses sobre o resultado quantitativo.

## Input necessário

Antes de começar, perguntar:
1. URL da variante de controle
2. URL da variante B (ou parâmetro de variação)
3. Resultado quantitativo que queremos explicar (ex: "lift de 12% concentrado em mobile")

## Checklist de heurísticas (rodar com agent-browser)

### Desktop (viewport 1440x900)

```
1. CTA principal: está acima da dobra sem scroll?
2. Contraste do botão CTA: passa WCAG AA (ratio >= 4.5:1)?
3. Hierarquia visual: onde o olho vai primeiro? (headline, imagem, CTA, preço)
4. Número de campos no form principal (se existir)
5. Presença de elementos de confiança: selos, reviews, garantia
6. Velocidade percebida: conteúdo acima da dobra carrega em < 2s?
```

### Mobile (viewport 390x844 — iPhone 14)

```
7. CTA principal: está no thumb zone (bottom 40% da tela)?
8. Tap targets: botões principais >= 44x44px?
9. Texto legível sem zoom: font-size >= 16px no corpo?
10. Form fields: teclado numérico disparado automaticamente em campos de número?
11. Distância entre links clicáveis: >= 8px de espaçamento?
```

## Instruções de execução

Para cada checklist item:
1. Usar `agent-browser` pra capturar screenshot no viewport correto
2. Avaliar o item: ✅ passa / ⚠️ atenção / ❌ falha
3. Para itens ❌: descrever o problema em 1 linha e sugerir hipótese de impacto

## Output esperado

```markdown
## Heuristic Scan — [URL]
**Viewport:** Desktop 1440 / Mobile 390

### Desktop
- ✅ CTA acima da dobra
- ✅ Contraste do botão: ratio 5.2:1
- ⚠️ Hierarquia visual: imagem de hero disputa atenção com CTA
- ❌ Form tem 6 campos (baseline: 4 campos)

### Mobile
- ✅ CTA no thumb zone
- ❌ Tap target do botão secundário: 32px (abaixo de 44px)
- ✅ Font-size: 16px

### Hipóteses para o resultado quantitativo
1. **Lift em mobile provavelmente vem do CTA no thumb zone** — na variante B o botão foi movido pra parte inferior da tela, posição natural do polegar. Isso explica preferência mobile.
2. **Desktop não moveu porque o form ainda tem 6 campos** — o CTA está visível mas a fricção no form não mudou. Testar redução de campos pode destravar desktop.
3. **Tap target do botão secundário pode estar causando cliques errados** — usuários tentando clicar em X podem estar ativando Y.
```

## Contexto esperado

- `agent-browser` disponível no Claude Code (MCP tool)
- URL acessível publicamente ou em ambiente de staging
- Resultado quantitativo da análise `/post-test-segments` disponível para correlacionar hipóteses
```

- [ ] **Step 2: Verificar que o arquivo está bem formado**

```bash
head -5 02-cro/demos/skills/heuristic-scan.md
```

Expected: frontmatter com `name: heuristic-scan` visível.

- [ ] **Step 3: Commit**

```bash
git add 02-cro/demos/skills/heuristic-scan.md
git commit -m "feat: add /heuristic-scan skill for CRO demo"
```

---

## Task 7: Hook PreToolUse de validação

Guard que impede análise de teste sem SRM check e sem período mínimo. Vai em `.claude/settings.json` do projeto.

**Files:**
- Modify: `.claude/settings.json` (criar se não existir)
- Create: `02-cro/demos/hooks/pre-tool-use-validation.json` (documentação do hook)

- [ ] **Step 1: Verificar se .claude/settings.json existe**

```bash
ls -la .claude/settings.json 2>/dev/null || echo "DOES NOT EXIST"
```

- [ ] **Step 2: Criar/atualizar .claude/settings.json com o hook**

Se não existe, criar:

```bash
mkdir -p .claude
cat > .claude/settings.json << 'EOF'
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash 02-cro/demos/hooks/validate-before-analysis.sh"
          }
        ]
      }
    ]
  }
}
EOF
```

Se já existe, adicionar o bloco `hooks` sem sobrescrever outras configurações (fazer manualmente ou via `jq`):

```bash
# verificar conteúdo atual
cat .claude/settings.json
# se tiver permissões, adicionar com jq:
jq '. + {"hooks": {"PreToolUse": [{"matcher": "Bash", "hooks": [{"type": "command", "command": "bash 02-cro/demos/hooks/validate-before-analysis.sh"}]}]}}' .claude/settings.json > /tmp/settings.json && mv /tmp/settings.json .claude/settings.json
```

- [ ] **Step 3: Criar o script de validação**

Create `02-cro/demos/hooks/validate-before-analysis.sh`:

```bash
#!/bin/bash
# Hook PreToolUse: valida pré-condições antes de análise de teste A/B
# Só ativa quando o comando contém palavras-chave de análise de CRO

COMMAND="$CLAUDE_TOOL_INPUT"

# verificar se é um comando de análise de teste
if ! echo "$COMMAND" | grep -qiE "(experiment_variant|test_results|converted|variant_b)"; then
  exit 0  # não é análise de teste, liberar
fi

echo "🔍 CRO Analysis Guard: verificando pré-condições..."

# 1. verificar se cro.db existe
if [ ! -f "cro.db" ]; then
  echo "❌ BLOQUEADO: cro.db não encontrado. Rode: duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql"
  exit 1
fi

# 2. verificar se SRM check foi executado (arquivo sentinela)
if [ ! -f ".cro-srm-checked" ]; then
  echo "⚠️ ATENÇÃO: SRM check não encontrado nesta sessão."
  echo "   Recomendado: invocar /srm-check antes de analisar resultados."
  echo "   (Para pular: touch .cro-srm-checked)"
  # avisa mas não bloqueia — usuário pode ter feito manualmente
  exit 0
fi

echo "✅ Pré-condições OK. Prosseguindo com análise."
exit 0
```

- [ ] **Step 4: Tornar o script executável**

```bash
chmod +x 02-cro/demos/hooks/validate-before-analysis.sh
```

- [ ] **Step 5: Criar documentação do hook**

Create `02-cro/demos/hooks/pre-tool-use-validation.json`:

```json
{
  "description": "Hook PreToolUse para validação de pré-condições em análises CRO",
  "trigger": "Qualquer comando Bash que contenha palavras-chave de análise de teste A/B",
  "checks": [
    "cro.db existe na pasta do projeto",
    "SRM check foi executado nesta sessão (.cro-srm-checked sentinela)"
  ],
  "behavior": {
    "cro.db ausente": "BLOQUEIA com mensagem de setup",
    "SRM não executado": "AVISA mas não bloqueia"
  },
  "demo_note": "Este hook é a materialização de 'Immutable by instruction is not enough' — é um guardrail em código, não em prompt"
}
```

- [ ] **Step 6: Testar o hook**

```bash
# simular comando de análise sem cro.db
unset CLAUDE_TOOL_INPUT
CLAUDE_TOOL_INPUT="SELECT * FROM test_results WHERE experiment_variant = 'variant_b'" bash 02-cro/demos/hooks/validate-before-analysis.sh
```

Expected com cro.db ausente: `❌ BLOQUEADO: cro.db não encontrado`
Expected com cro.db presente + sem sentinela: `⚠️ ATENÇÃO: SRM check não encontrado`
Expected com cro.db + sentinela: `✅ Pré-condições OK`

- [ ] **Step 7: Commit**

```bash
git add .claude/settings.json 02-cro/demos/hooks/
git commit -m "feat: add PreToolUse validation hook for CRO analysis guard"
```

---

## Task 8: Demo runbook + bifurcations.md

Documento operacional para o palestrante executar as demos ao vivo sem hesitar.

**Files:**
- Create: `02-cro/demos/commands.md`
- Create: `02-cro/bifurcations.md`

- [ ] **Step 1: Criar commands.md (runbook ao vivo)**

Create `02-cro/demos/commands.md`:

```markdown
# CRO Demo Runbook — Palestra 02

> Sequência exata de comandos para executar ao vivo. Testar D-1 completo.

## Setup pré-palestra (D-1)

```bash
cd /Users/lucianfialho/Code/talks-2026
duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql
# verificar: deve retornar "Setup completo." com contagem de usuários
```

## Ato 1 — Abertura (wterm)

URL: https://wterm.vercel.app (ou instância local)
Ação: abrir browser, navegar pra wterm, mostrar Claude Code iniciando

Fallback se wterm offline: abrir terminal local, digitar `claude` na pasta do projeto

## Ato 2 — DuckDB (demo ao vivo)

```bash
# mostrar criação do banco e primeira query — slide esquerda, terminal direita
duckdb cro.db
```

```sql
-- no prompt do duckdb:
SELECT COUNT(*) AS total_events, COUNT(DISTINCT user_pseudo_id) AS total_users
FROM events;
```

Expected: ~110k eventos, ~8600 usuários

```sql
-- mostrar o conceito de "LLM Wiki" — dado estruturado
DESCRIBE events;
```

## Ato 3A — Bot filtering

```sql
-- na sessão duckdb cro.db:
SELECT user_pseudo_id, COUNT(*) AS hits, AVG(session_duration) AS avg_dur
FROM events WHERE event_name = 'session_start'
GROUP BY user_pseudo_id
HAVING hits > 50 OR avg_dur < 1
LIMIT 10;
```

Expected: ~200 usuários com padrão de bot (hits > 60, duration = 0)

```sql
-- antes vs depois
SELECT 'bruto' AS estado, COUNT(DISTINCT user_pseudo_id) AS users FROM events WHERE event_name = 'session_start'
UNION ALL
SELECT 'limpo', COUNT(DISTINCT user_pseudo_id) FROM events_clean WHERE event_name = 'session_start';
```

## Ato 3B — SRM check

Claude Code invoca `/srm-check`:

```
/srm-check
```

Expected no output: SRM detectado em variant_b (400 usuários extras injetados)

## Ato 3C — Hook ao vivo

```bash
# mostrar o hook sendo ativado
CLAUDE_TOOL_INPUT="SELECT * FROM test_results WHERE experiment_variant = 'variant_b'" \
bash 02-cro/demos/hooks/validate-before-analysis.sh
```

## Ato 4A — Segmentação pós-teste

Claude Code invoca `/post-test-segments` após SRM check aprovado:

```
# criar sentinela de SRM aprovado para demo
touch .cro-srm-checked

/post-test-segments
```

Expected: mobile/variant_b com CR ~20% acima de mobile/control

## Ato 4B — Heuristic scan

```
/heuristic-scan
```

Fornecer URL de staging quando solicitado.

## Ato 5 — Automação

```
/schedule every monday at 8am: /cro-weekly-pipeline

/loop 2h: /srm-check
```

Mostrar na tela e explicar — não precisa executar de verdade.

## Bifurcação #1 — Dataset ao vivo (Ato 3, se escolhida)

```bash
# pipeline completo do bruto ao clean, com log de cada etapa
duckdb cro.db << 'SQL'
SELECT 'Passo 1: total bruto' AS etapa, COUNT(DISTINCT user_pseudo_id) AS usuarios FROM events WHERE event_name = 'session_start'
UNION ALL
SELECT 'Passo 2: bots identificados', COUNT(DISTINCT user_pseudo_id) FROM sessions WHERE hit_count > 50 OR session_duration = 0
UNION ALL
SELECT 'Passo 3: dado limpo', COUNT(DISTINCT user_pseudo_id) FROM events_clean WHERE event_name = 'session_start';
SQL
```

## Bifurcação #2 — Heuristic scan completo (Ato 4, se escolhida)

Abrir `agent-browser` manualmente em URL de staging e rodar o checklist do `/heuristic-scan` item por item.

## Fallback total (sem internet)

```bash
# usar dataset local já carregado — tudo roda offline com duckdb local
duckdb cro.db "SELECT 'offline OK' AS status;"
```
```

- [ ] **Step 2: Criar bifurcations.md**

Create `02-cro/bifurcations.md`:

```markdown
---
talk: 02-cro
bifurcations: 2
formato: hand-raise (online = chat ou poll na plataforma)
---

# Bifurcações — Palestra 02 (CRO)

Ambas as bifurcações são sobre **profundidade técnica** — não mudam a narrativa, só o quanto você vai a fundo na demo.

---

## Bifurcação #1 — Ato 3, após 3C (guardrails)

**Gatilho:** fim da explicação do hook `PreToolUse`

**Pergunta:** *"Quer ver os 3 passos — bot filter, SRM check, guardrail — rodando num dataset real do início ao fim? Ou segue pra análise pós-teste?"*

**Caminho Raso (1-2 min):**
- Mostrar o output final da view `events_clean` já pronta
- *"O pipeline inteiro produziu isso. Os scripts estão no repo."*
- Tempo salvo: ~4 min → vai pro Ato 4

**Caminho Fundo (5 min):**
- Executar `Bifurcação #1 — Dataset ao vivo` do `commands.md`
- Mostrar cada etapa com contagem: bruto → bots → clean
- Explicar o threshold de decisão (50 hits OU duration=0)
- Rodar SRM check com output ao vivo
- Mostrar hook sendo disparado e bloqueando

**Convergência:** ambos chegam no mesmo ponto — dataset `events_clean` validado, pronto para `/post-test-segments`

---

## Bifurcação #2 — Ato 4, após 4A (pós-teste)

**Gatilho:** após mostrar segmentação por device do resultado

**Pergunta:** *"Quer ver o checklist completo de heurísticas rodando ao vivo numa página real?"*

**Caminho Raso (1 min):**
- Mostrar exemplo de output do `/heuristic-scan` já pronto (screenshot ou texto)
- *"É isso que sai. O checklist tem 11 itens, está no repo."*
- Tempo salvo: ~3 min → vai pro Ato 5

**Caminho Fundo (4 min):**
- Executar `Bifurcação #2 — Heuristic scan completo` do `commands.md`
- `agent-browser` abre URL, captura screenshots desktop e mobile
- Rodar ao menos 5 dos 11 itens do checklist ao vivo
- Correlacionar resultado heurístico com o lift quantitativo do Ato 4A

**Convergência:** ambos chegam no Ato 5 (automação)

---

## Buffer de tempo

| Cenário | Tempo total |
|---|---|
| Ambas rasas | ~42 min → 8 min de Q&A |
| Bif #1 fundo, #2 raso | ~46 min → 4 min buffer |
| Bif #1 raso, #2 fundo | ~45 min → 5 min buffer |
| Ambas fundas | ~50 min exato |
```

- [ ] **Step 3: Commit**

```bash
git add 02-cro/demos/commands.md 02-cro/bifurcations.md
git commit -m "feat: add demo runbook and bifurcations for CRO talk"
```

---

## Task 9: Script narrativo completo — script.md

O script em voz do palestrante, blog-post style, ~5000-7000 palavras. Segue o `script-outline.md` ato a ato.

**Files:**
- Create: `02-cro/script.md`

**Estilo:** Mesmo voice target da palestra 01 — conversacional, primeira pessoa, sem corporatês. Diferença: audiência CRO é mais técnica, pode-se usar termos como SRM, chi-quadrado, p-value sem explicar do zero.

- [ ] **Step 1: Gerar estrutura do script**

Create `02-cro/script.md` com a estrutura base:

```markdown
---
titulo: "CRO com Claude Code: do dado bruto à decisão"
talk: 02
status: draft-01
voice_target: Conversacional, direto, técnico quando necessário. Zero hype de IA.
duracao_estimada: 50min
audiencia: CRO community — misto técnico/ferramenta
---

# Script — Palestra 02 (CRO)

> Versão para leitura e ensaio. Cada ato tem tempo-alvo entre colchetes.

---

## Ato 1 — Abertura: Claude Code no browser [5 min]

[AÇÃO: abrir wterm no browser, mostrar Claude Code iniciando]

*"Isso é Claude Code. No browser. Sem instalar nada."*

[pausa 3 segundos]

*"Tudo que vocês vão ver hoje acontece aqui. Dados reais de GA4, limpeza, análise de A/B — no browser. Ou no terminal local, que é a mesma coisa."*

*"Mas antes de qualquer demo — deixa eu te fazer uma pergunta. Quanto do seu tempo de análise você realmente gasta analisando?"*

[SLIDE: 60-70% em prep]

*"Esse número aí é o que eu vejo quando a galera cronometra o próprio trabalho. Sessenta a setenta por cento do tempo vai pra preparação — puxar dado, verificar se o dado tá certo, limpar o que não presta, configurar o contexto pra análise. E aí você tem vinte, trinta por cento do tempo pra fazer a parte que importa."*

*"O problema de CRO não é falta de ferramenta de análise. A gente tem Hotjar, VWO, ABTasty, GA4, BigQuery, sei lá mais o quê. O problema é a camada anterior a tudo isso. E é exatamente aí que o Claude Code resolve."*

---

## Ato 2 — Por que o contexto importa [8 min]

[SLIDE: comparativo de contexto — 200k vs 1M tokens]

*"Quando você tenta jogar um CSV de GA4 no ChatGPT — e vai me dizer que você nunca tentou — o que acontece? Arquivo muito grande. Ou ele aceita mas você tá jogando uma amostra. Você não tá analisando o dado, você tá analisando um recorte do dado."*

*"ChatGPT, Cursor — janela de contexto de 200 mil tokens. Um dataset de GA4 de 30 dias com eventos completos — session, page_view, click, conversion — facilmente 800 mil tokens. Você vai samplear. Sempre."*

*"Claude Code tem 1 milhão de tokens de contexto. Na prática: você joga o dataset de um mês inteiro sem samplear. A análise vê o dado real, não um recorte que você espera que seja representativo."*

[AÇÃO: abrir terminal, mostrar DuckDB]

*"E pra aproveitar esse contexto de verdade, você precisa de uma camada intermediária. Não joga CSV cru direto no Claude — você estrutura o dado antes."*

[SLIDE: conceito LLM Wiki do Karpathy]

*"O Karpathy tem um conceito que eu gosto muito — dado estruturado como contexto navegável pra LLM. Não é exportar CSV e torcer. É preparar o dado de um jeito que o modelo consiga raciocinar em cima com precisão."*

*"A implementação prática disso aqui é o DuckDB."*

[AÇÃO: criar cro.db ao vivo]

```bash
duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql
```

*"DuckDB é um SQLite analítico. Zero setup, roda na pasta do projeto, fala SQL padrão, aguenta dataset grande. Você roda isso aqui e em 10 segundos você tem um banco local com seus eventos do GA4, pronto pra consultar e pra jogar no contexto do Claude."*

[AÇÃO: rodar DESCRIBE events]

*"Isso aqui é o seu cache local entre a API e o Claude. Você puxou via gmp-cli, caiu no DuckDB, o Claude acessa com SQL e raciocina em cima do dado completo. É isso."*

---

## Ato 3 — A limpeza na prática [15 min]

### 3A — Bot filtering

[SLIDE: "15-30% dos seus eventos são lixo"]

*"Deixa eu te mostrar uma coisa no dado que a gente acabou de carregar."*

[AÇÃO: rodar query de bot detection]

*"Esse usuário aqui — 73 hits, session_duration zero. E esse aqui — 91 hits, zero segundos de sessão. Isso é bot. E o GA4 padrão não filtra."*

*"A pergunta que você devia estar fazendo é: qual é o seu baseline de conversão com esses bots dentro? Porque eles não convertem, mas eles inflam o denominador. Seu CR real pode ser mais alto do que você acha."*

[AÇÃO: mostrar before/after da view events_clean]

### 3B — SRM check

[SLIDE: diagrama SRM]

*"Segundo problema. Você tá rodando um teste A/B e a variante B recebeu 23% mais tráfego que o control. Isso é SRM — Sample Ratio Mismatch. O seu teste tá contaminado antes de você olhar pra conversão."*

[AÇÃO: invocar /srm-check]

*"É um comando. O Claude calcula chi-quadrado, devolve o veredicto. Não tem desculpa pra declarar vencedor sem ter rodado isso antes."*

### 3C — Guardrails como código

[SLIDE: "Immutable by instruction is not enough"]

*"Na palestra passada eu falei sobre o mmm-research — o agente que aprendeu a trapacear porque a restrição tava em instrução, não em código. Aqui é a mesma coisa."*

*"Você não fala pro Claude 'não analise dado com SRM'. Você escreve o hook que bloqueia antes de qualquer análise rodar."*

[AÇÃO: mostrar o hook sendo disparado]

*"Isso não é prompt. Isso é código. O Claude não consegue racionalizar por volta disso."*

[BIFURCAÇÃO #1]

---

## Ato 4 — Análise e heurísticas [12 min]

### 4A — Análise pós-teste

[AÇÃO: invocar /post-test-segments]

*"Agora que o dado tá limpo e o SRM tá checado — resultado."*

*"O lift agregado é 12%. Mas olha aqui — mobile tá com 20% de lift. Desktop não moveu. Tablet, amostra pequena."*

*"Se você implementar pra todo mundo com base nos 12% agregados, o ganho real é 6%. Você vai entregar pra engenharia um número errado e cobrar resultado de uma mudança que foi metade do esperado."*

*"Esse insight existe no dado. Mas ninguém vai atrás porque a análise padrão para no agregado."*

### 4B — Heurísticas com agent-browser

[SLIDE: quantitativo diz O QUÊ, heurística diz POR QUÊ]

*"Por que mobile moveu e desktop não? Os dados quantitativos não respondem isso. Mas uma varredura heurística sim."*

[AÇÃO: invocar /heuristic-scan]

*"O agent-browser abre a variante B, faz a varredura automática — CTA acima da dobra, contraste, thumb zone em mobile, friction no form — e devolve hipóteses."*

*"Variante B moveu o CTA pro thumb zone em mobile. Isso explica 80% do lift. O form ainda tem 6 campos — isso segura desktop."*

*"Você não precisou abrir o Hotjar. Não precisou assistir gravação de sessão. O agente fez a varredura e devolveu as hipóteses."*

[BIFURCAÇÃO #2]

---

## Ato 5 — Automação + fechamento [10 min]

[SLIDE: stack diagram completo]

*"O que vocês viram hoje — bot filter, SRM check, segmentação pós-teste, heurística — são passos manuais que você faz uma vez por semana em cada teste ativo."*

[AÇÃO: mostrar /schedule e /loop]

```
/schedule every monday at 8am: /cro-weekly-pipeline

/loop 2h: /srm-check
```

*"Todo teste ativo, verificado de 2 em 2 horas. Toda segunda às 8 da manhã, relatório completo com limpeza, SRM status e segmentação pronto na sua caixa."*

*"Você não faz mais isso manualmente. O agente faz enquanto você dorme."*

[SLIDE: 4 skills + QR code]

*"As 4 skills que eu mostrei hoje estão no repo. `/srm-check`, `/post-test-segments`, `/cro-weekly-pipeline`, `/heuristic-scan`. Markdown, faz clone, funciona em qualquer projeto Claude Code."*

*"E se você quer isso pré-montado sem montar o stack — analytics-copilot em beta. QR code aí. Mesma newsletter da palestra passada."*

*"Obrigado."*
```

- [ ] **Step 2: Revisar timing via leitura cronometrada**

Ler cada ato em voz alta e cronometrar. Ajustar onde necessário. Registrar em `02-cro/notes/rehearsal-log.md`:

```bash
cat > 02-cro/notes/rehearsal-log.md << 'EOF'
# Rehearsal Log — Palestra 02 (CRO)

## Ensaio template

| Ato | Tempo alvo | Tempo real | Delta | Ajuste |
|---|---|---|---|---|
| 1 — Abertura wterm | 5 min | | | |
| 2 — Contexto + DuckDB | 8 min | | | |
| 3 — Limpeza + Bif #1 | 15 min | | | |
| 4 — Análise + Bif #2 | 12 min | | | |
| 5 — Automação + CTA | 10 min | | | |
| **Total** | **50 min** | | | |

## Notas
-
EOF
```

- [ ] **Step 3: Commit**

```bash
git add 02-cro/script.md 02-cro/notes/rehearsal-log.md
git commit -m "feat: add CRO talk script draft and rehearsal log template"
```

---

## Task 10: Validação final do stack técnico

Garantir que todo o stack de demo funciona de ponta a ponta antes do ensaio.

**Files:**
- No new files. Validação de tudo que foi criado.

- [ ] **Step 1: Testar pipeline completo do zero**

```bash
cd /Users/lucianfialho/Code/talks-2026
rm -f cro.db .cro-srm-checked

# setup
python3 02-cro/demos/data/generate-sample-data.py
duckdb cro.db < 02-cro/demos/data/setup-duckdb.sql
```

Expected: `Setup completo.` com ~8600+ usuários.

- [ ] **Step 2: Verificar bot filtering**

```bash
duckdb cro.db "
SELECT 'bruto' AS estado, COUNT(DISTINCT user_pseudo_id) AS u FROM events WHERE event_name='session_start'
UNION ALL
SELECT 'limpo', COUNT(DISTINCT user_pseudo_id) FROM events_clean WHERE event_name='session_start';
"
```

Expected: limpo deve ter ~200 usuários a menos que bruto.

- [ ] **Step 3: Verificar SRM nos dados**

```bash
duckdb cro.db "
SELECT experiment_variant, COUNT(DISTINCT user_pseudo_id) AS users
FROM events_clean WHERE event_name='session_start' AND experiment_variant IS NOT NULL
GROUP BY experiment_variant;
"
```

Expected: variant_b deve ter mais usuários que control (SRM injetado).

- [ ] **Step 4: Verificar segmentação pós-teste**

```bash
duckdb cro.db "
SELECT device_category, experiment_variant,
       COUNT(DISTINCT user_pseudo_id) AS users,
       ROUND(SUM(converted)*100.0/COUNT(DISTINCT user_pseudo_id),2) AS cr_pct
FROM events_clean WHERE event_name='session_start'
GROUP BY device_category, experiment_variant
ORDER BY device_category, experiment_variant;
"
```

Expected: mobile/variant_b deve ter CR ~20% maior que mobile/control.

- [ ] **Step 5: Verificar hook**

```bash
CLAUDE_TOOL_INPUT="SELECT * FROM test_results WHERE experiment_variant='variant_b'" \
bash 02-cro/demos/hooks/validate-before-analysis.sh
```

Expected: `⚠️ ATENÇÃO: SRM check não encontrado`

```bash
touch .cro-srm-checked
CLAUDE_TOOL_INPUT="SELECT * FROM test_results WHERE experiment_variant='variant_b'" \
bash 02-cro/demos/hooks/validate-before-analysis.sh
```

Expected: `✅ Pré-condições OK`

- [ ] **Step 6: Verificar que todos os arquivos de skill existem**

```bash
ls -la 02-cro/demos/skills/
```

Expected: 4 arquivos `.md` — `srm-check.md`, `post-test-segments.md`, `cro-weekly-pipeline.md`, `heuristic-scan.md`

- [ ] **Step 7: Commit final de validação**

```bash
git add -A
git commit -m "chore: validate CRO demo stack end-to-end"
```

---

## Ordem de execução recomendada

Tasks independentes — podem rodar em paralelo por subsistema:

**Subsistema A (demos técnicas):** Tasks 1 → 2 → 3 → 4 → 5 → 6 → 7 → 10
**Subsistema B (conteúdo):** Task 8 → Task 9
**Blocker:** Task 10 depende de Tasks 1-7 completas.

---

## Gaps de produção não cobertos neste plano

Os itens abaixo dependem de contexto externo e estão fora do escopo:

- [ ] Testar `wterm` (vercel-labs/wterm) com Claude Code real — requer conta Vercel
- [ ] `gmp-cli` configurado com credenciais reais — requer setup manual
- [ ] `agent-browser` apontado para URL de staging real — requer ambiente de teste disponível
- [ ] Slides visuais (deck) — issue separada, seguir padrão da palestra 01
- [ ] Confirmar referência exata do "LLM Wiki" do Karpathy antes do ensaio
